import aliases from "../../res/countryFlags.json";

/**
 * English answer aliases are the canonical country dataset. Multiple entries
 * allow abbreviations and common alternative names such as USA or DRC.
 */
export const EN_COUNTRY_NAMES: Record<string, string[]> = aliases;
